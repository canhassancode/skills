---
name: inbox
description: Deprecated — superseded by /ingest.
disable-model-invocation: true
---

# Inbox (deprecated)

Superseded by [`ingest`](../../personal/ingest/SKILL.md), which now drains `~/Obsidian/Inbox/` as part of its own flow: read the top capture, ingest it or bin it. `inbox` carried six destinations for a capture and five of them no longer exist; a standing triage skill in front of a queue that was never drained was ceremony, not a workflow.

Run `/ingest` instead.
