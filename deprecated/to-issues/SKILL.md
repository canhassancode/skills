---
name: to-issues
description: Deprecated — superseded by /cut.
disable-model-invocation: true
---

# to-issues (deprecated)

Superseded by [`cut`](../../engineering/cut/SKILL.md). "Issue" was biased toward GitHub/Linear; "ticket" is tracker-neutral. `cut` breaks a settled alignment into tracer-bullet vertical slices, each declaring its **blocking edges**, and works either against a real tracker (native blocking links) or a local `tickets.md` file. Every slice lands at `ready-to-build` — no `/triage` step on the planning lane.

Run `/cut` instead.
