---
name: grill-me
description: Deprecated — superseded by /align.
disable-model-invocation: true
---

# grill-me (deprecated)

Superseded by [`align`](../../engineering/align/SKILL.md).

Its whole body was a delegation to `/grilling`, plus a second-brain sweep. `/align` is the entry point directly rather than a wrapper over a shared primitive — the primitive was the thing that failed — and its facts come from delegated legwork recorded in the **Alignment artefact** rather than from a vault probe at open.

Run `/align` instead.
